type: object
properties:
  currentEmail:
    type: string
    format: email
    description: Current email address of the user
  newEmail:
    type: string
    format: email
    description: New email address of the user
required:
  - currentEmail
  - newEmail