type: object
properties:
  type:
    type: string
    description: "The type of the feedback. The type can only be one of the following: 
          PROPOSITION, ISSUE, FEEDBACK."
    enum:
      - PROPOSITION
      - ISSUE
      - FEEDBACK
  text:
    type: string
    description: The text of the feedback
required:
  - type
  - text