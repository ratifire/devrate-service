type: object
properties:
  currentPassword:
    type: string
    description: Current password of the user
  newPassword:
    type: string
    description: New password of the user (pattern "^(?=.*[A-Z])(?=.*\\d)(?=.*[^A-Za-z\\d\\s])(?!.*\\s).{6,}$")
required:
  - currentPassword
  - newPassword