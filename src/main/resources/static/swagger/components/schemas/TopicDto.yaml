type: object
properties:
  topicName:
    type: integer
    format: int64
    description: The unique identifier for the chat topic
    required: true
  userId:
    type: integer
    format: int64
    description: The ID of the user associated with the chat topic
    required: true
  lastMessage:
    type: string
    description: The content of the last message
    required: true
  lastMessageDate:
    type: string
    format: date-time
    description: The timestamp of the last message
    required: true
