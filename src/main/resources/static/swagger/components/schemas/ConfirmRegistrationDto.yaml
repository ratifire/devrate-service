type: object
properties:
  confirmationCode:
    type: string
    description: The unique confirmation code received by the email.
  email:
    type: string
    description: The email address of the user who is confirming their registration.
required:
  - confirmationCode
  - email