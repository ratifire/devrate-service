type: object
description: "Data Transfer Object representing a user main mastery skills."
properties:
  specializationDto:
    type: object
    $ref: '../../components/schemas/SpecializationDto.yaml'
    description: "Details about the user's specialization."
  masteryDto:
    type: object
    $ref: '../../components/schemas/MasteryDto.yaml'
    description: "Information about the user's mastery."
  skillDto:
    type: object
    $ref: '../../components/schemas/SkillDto.yaml'
    description: "List of skills associated with the user's mastery."
  
