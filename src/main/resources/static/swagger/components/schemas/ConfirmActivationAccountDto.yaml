type: object
properties:
  activationCode:
    type: string
    description: Activation code sent to user's email
  password:
    type: string
    description: Password of the user (only for non-SSO users)
required:
  - activationCode