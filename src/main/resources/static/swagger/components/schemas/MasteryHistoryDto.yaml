type: object
properties:
  masteryId:
    type: integer
    format: int64
    description: ID of the mastery
  date:
    type: string
    format: date
    description: The date of the history record in ISO format (yyyy-MM-dd)
  hardSkillMark:
    type: number
    format: double
    description: Hard skill mark
  softSkillMark:
    type: number
    format: double
    description: Soft skill mark
required:
  - masteryId
  - date
  - hardSkillMark
  - softSkillMark
