type: object
properties:
  id:
    type: integer
    format: int64
    description: The unique identifier for the specialization
    readOnly: true
  name:
    type: string
    description: The name of the specialization
  mainMasteryLevel:
    type: integer
    description: The main mastery level of the specialization (JUNIOR (1), MIDDLE (2), SENIOR (3))
  mainMasteryId:
    type: integer
    format: int64
    description: The main mastery id of the specialization
    readOnly: true
  main:
    type: boolean
    description: Whether the specialization is the main specialization
  completedInterviews:
    type: integer
    format: int64
    description: The number of completed interviews for the specialization
    readOnly: true
  conductedInterviews:
      type: integer
      format: int64
      description: The number of conducted interviews for the specialization
      readOnly: true
required:
  - name
  - main