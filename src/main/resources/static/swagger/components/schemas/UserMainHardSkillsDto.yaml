type: object
description: "Data Transfer Object representing a user main mastery hard skills."
properties:
  specializationName:
    type: string
    description: "Name of the user's specialization."
  isMainSpecialization:
    type: boolean
    description: "Flag indicating whether the current specialization is the user's main specialization."
  masteryName:
    type: string
    description: "Name of the user's specialization main mastery."
  hardSkills:
    type: array
    description: "List of hard skills associated with the user's main mastery."
    items:
      $ref: '../../components/schemas/SkillDto.yaml'

