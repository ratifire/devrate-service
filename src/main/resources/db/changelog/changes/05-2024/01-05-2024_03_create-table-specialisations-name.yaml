databaseChangeLog:
  - changeSet:
      id: 01-05-2024_03_create-table-specialisations-name
      author: Maksim Matveychuk
      preConditions:
        - onFail: MARK_RAN
          comments: |
            Checks if the "specialisations_name" table already exists.
            Marks the changeSet as executed if the table exists.
          not:
            tableExists:
              tableName: specialisations_name
      changes:
        - createTable:
            tableName: specialisations_name
            columns:
              - column:
                  name: id
                  type: BIGSERIAL
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: specialisation_name
                  type: VARCHAR(255)
                  constraints:
                    nullable: false