package com.ratifire.devrate.enums;

/**
 * Enum representing the type of notification, used to categorize notifications by their purpose.
 */
public enum NotificationType {

  GREETING,
  INTERVIEW_REQUEST_EXPIRED,
  INTERVIEW_REJECTED,
  INTERVIEW_SCHEDULED,
}
